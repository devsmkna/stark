Ecco il punto della situazione, letto dalla lista condivisa in `.stark/todo.json` (la stessa che vedi nella sidebar Todo).

**Una sola cosa è ancora aperta: «Accesso da fuori casa senza Tailscale»** — 1 su 3.

- ✅ Perimetro dichiarato con `STARK_PUBLIC_HOST` — fatto.
- 🔨 **Traefik sul VPS con Let's Encrypt e mTLS** — è qui che siamo adesso, l'unica cosa in lavorazione.
- ⬜ Misurare il tunnel con `npm run tunnel` — non ancora iniziata.

**Già chiusa:** «Bugfix della barra di stato da telefono» (i tre chip alti uguali).

Cosa ho fatto e perché: ho solo **letto** le liste con `todo.py list`, senza modificare niente. Mi hai chiesto un punto della situazione, non un cambio di stato.
