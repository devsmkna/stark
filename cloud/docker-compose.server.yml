# Il server cloud di STARK sul server dedicato (45.77.53.112), dietro Traefik.
#
# Questo compose NON builda il cloud: l'immagine arriva già caricata con `docker load`
# (vedi deploy-dev.sh). Avvia il server cloud + il Postgres che gli serve + Traefik,
# più la homepage statica dell'apex.
#
#   docker compose -f docker-compose.dev.yml up -d
#
# Due nomi, due router, due certificati:
#   cloud.starkapp.dev → il server cloud (l'API con cui parlano i daemon)
#   starkapp.dev       → la homepage (cloud/www, un segnaposto finché non c'è la landing)
#
# L'apex sta su Cloudflare **proxied**: il certificato Let's Encrypt sull'origin serve
# a rendere onesta anche la tratta Cloudflare→server (modalità SSL «Full (strict)» sul
# pannello Cloudflare — dal nostro lato è pronta, l'interruttore è di là). Il CNAME
# `cloud` invece è nato DNS-only, quindi lì i client parlano dritti con Traefik.
#
# La porta 8787 resta pubblicata NUDA per la transizione: i daemon già installati
# hanno ancora `http://45.77.53.112:8787` come default. Quando tutte le macchine
# saranno passate a https://cloud.starkapp.dev, quella riga va tolta: è HTTP in
# chiaro con dentro password e token.
#
# Dati (account, sessioni, DB) in bind mount su /opt/stark-cloud: file normali del
# server, visibili e backuppabili. `letsencrypt/` conserva il certificato: si tiene
# per non richiederlo a ogni ricambio del container (Let's Encrypt ha rate limit).

services:
  cloud:
    image: stark-cloud:dev
    restart: unless-stopped
    depends_on:
      - db
    ports:
      - "8787:8787"
    environment:
      - PORT=8787
      - DATABASE_URL=postgres://stark:stark@db:5432/stark
    volumes:
      - /opt/stark-cloud/data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.stark-cloud.rule=Host(`cloud.starkapp.dev`)"
      - "traefik.http.routers.stark-cloud.entrypoints=websecure"
      - "traefik.http.routers.stark-cloud.tls.certresolver=letsencrypt"
      - "traefik.http.services.stark-cloud.loadbalancer.server.port=8787"

  home:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - /opt/stark-cloud/www:/usr/share/nginx/html:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.stark-home.rule=Host(`starkapp.dev`)"
      - "traefik.http.routers.stark-home.entrypoints=websecure"
      - "traefik.http.routers.stark-home.tls.certresolver=letsencrypt"
      - "traefik.http.services.stark-home.loadbalancer.server.port=80"

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      - POSTGRES_USER=stark
      - POSTGRES_PASSWORD=stark
      - POSTGRES_DB=stark
    volumes:
      - /opt/stark-cloud/pgdata:/var/lib/postgresql/data

  traefik:
    image: traefik:v3
    restart: unless-stopped
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      # Il traffico vero arriva su 443; l'80 serve alla sfida ACME (che Traefik
      # intercetta prima del redirect) e a chi scrive l'indirizzo senza schema.
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.email=devops@digitizers.it"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
      - "/opt/stark-cloud/letsencrypt:/letsencrypt"
